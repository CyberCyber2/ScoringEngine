Only the installer is needed for debian. Just drag this onto a debian system(or download it using wget). Then when the people want to start their image, have them run it. It will automatically install and obfuscate the ScoringEngine.
