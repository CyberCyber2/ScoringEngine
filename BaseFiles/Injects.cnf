[General]
	checkInterval=5
	ftpFile=FTPCHECK.txt
	ftpFileHash=da39a3ee5e6b4b0d3255bfef95601890afd80709
	ftpUSR=client
	ftpPWD=client
	ftpPath=/home/client/
	serverFTPDir=/home/server/Desktop
	apacheCheck=Check2132
	apacheWebsite=index.html
	dataDirectory=/home/server/Desktop/data/
	mySQL_Query="SELECT name, address FROM customers"
	smbUSR=anon
	smbPWD=anon
	smbSHR=Public

[Injects]
	#FTP 
	#SMB
	smbCurrInject=1 #Download (teamname.txt) from smbSHR
	#APACHE

[Desc]
	Inject1="Linux - Configure VSFTPD so that the file stored at (url) is placed in the default FTP share"
	Inject2="Linux2 - Change the default SSH port to 2222"
	Inject3="Linux3 - Create the SAMBA share called Public and allow guests to access the share"
